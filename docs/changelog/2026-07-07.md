# 2026-07-07

## M1–M4 implemented
- **What:** Push-to-talk capture (hotkey + cpal + resample), whisper.cpp STT with built-in Silero VAD and resumable model downloads, agent sidecar with token-authenticated WebSocket (spawn/handshake/respawn + privileged core channel), LangGraph agent loop with Groq/Ollama provider abstraction, built-in open_path/insert_text tools, MCP manager, keychain secret storage, confirm-gate wrapper (default-deny), sidebar+transcript UI, overlay mic pill, eval harness with CI hook.
- **Why:** PRD build order M1–M4; M3 exit test passed (auth rejection + round-trip); M2 latency verified via spike (base.en 1.04s for 12.6s audio on dev machine).
- **Files:** src-tauri/src/** (hotkey, tray, windows, sidecar, core_client, settings, commands/*, audio/*), sidecar/src/** (server, auth, agent/*, mcp/*, core-bridge), sidecar/evals/, frontend/src/{windows,components/vox,stores,lib}/**, packages/protocol, .github/workflows/ci.yml.
- **Breaking:** Node ≥22 now required for dev; Vite dev port is 3000 (tauri devUrl updated).

## M5–M8 surfaces implemented
- **What:** Connectors tab (CRUD, verbatim-command review, test + tool list, GitHub preset with PAT→keychain), confirm-before-acting overlay card wired to LangGraph interrupts, secure-input fallback notices, Settings view (hotkey recorder, provider toggle, STT model select, Groq key paste), macOS permissions onboarding banner, Playwright smoke tests, ollama-mode eval harness.
- **Why:** completes the PRD P5–P10 feature set pending live validation.
- **Files:** src-tauri/src/{core_client.rs,windows.rs,commands/{connectors,permissions}.rs}, sidecar/src/index.ts, sidecar/evals/, frontend/src/windows/{connectors,settings,overlay,main}.tsx, frontend/src/components/vox/permissions-banner.tsx, packages/protocol.
- **Breaking:** none.

## Live agent validation (Ollama qwen3:1.7b, no cloud key needed)
- **What:** S2 plumbing PASS (utterance → open_path(~/Documents) → core channel → result). S4/S6/S8 deny path PASS (write_file and create_directory each individually gated; deny cancels cleanly; sandbox untouched). Fixed a real bug: LangGraph treats `Command({resume: false})` as empty input — resume value is now an object `{approved}`. S4 approve path PASS: after explicit approval the gated write_file executed and meeting-notes.md was created with the exact requested content.
- **Files:** sidecar/src/agent/{graph,runner}.ts

## Setup guide + architecture walkthrough added
- **What:** docs/SETUP.md (prerequisites, install, permissions, provider/connector configuration, usage, troubleshooting table, dev commands) and docs/HOW-IT-WORKS.md (three-process architecture, life-of-a-voice-command, message plumbing, security model, disk layout); linked from README.
- **Why:** user requested a complete setup guide and how-it-works documentation.
- **Files:** docs/SETUP.md, docs/HOW-IT-WORKS.md, README.md.
