# 2026-07-12

## Cleanup + CI gates
- **What:** removed template leftovers (browsers.test.ts asserting the old template title, the nested `frontend/.github` workflow GitHub never reads, `VITE_PLACEHOLDER` env validation + `@t3-oss/env-core`), fixed `.env.example` HOST, dropped a stale `dead_code` allow. CI now lints all packages, runs the protocol unit tests, and has a chromium Playwright smoke job.
- **Files:** frontend/__tests__/, frontend/src/{env.ts,vite-env.d.ts}, .github/workflows/ci.yml, src-tauri/src/audio/capture.rs.
- **Breaking:** none.

## Operational floor
- **What:** `tauri-plugin-log` replaces env_logger — rotating file log (2 MB keep-one) in the app log dir plus stderr, with sidecar stderr flowing in; a chained panic hook records panics to the file; `restart_sidecar` command revives the agent after the 5-respawn dead end, exposed in the tray ("Restart agent", "Open logs folder") and as a sidebar restart link.
- **Files:** src-tauri/src/{lib.rs,sidecar.rs,tray.rs}, src-tauri/Cargo.toml, frontend/src/components/vox/sidebar.tsx.
- **Breaking:** none.

## Conversation UX
- **What:** token-level streaming (`assistant_delta` frames from LangGraph 'messages' stream mode; final `assistant_message` stays authoritative), a "new conversation" reset (fresh thread id + cleared transcript), and opt-in transcript persistence behind Settings → Privacy (last 200 turns in localStorage, default off, disabling erases).
- **Files:** packages/protocol/src/index.ts, sidecar/src/{agent/runner.ts,index.ts}, frontend/src/{stores/session.ts,lib/agent-socket.ts,components/vox/transcript.tsx,windows/settings.tsx}.
- **Breaking:** none (new frame type is additive).

## Sidecar packaging (closes the distribution blocker)
- **What:** esbuild bundles the agent to a single `sidecar/dist/index.mjs` (3.1 MB), shipped as a Tauri resource and launched in packaged builds by system Node ≥20 (`find_node()` scans PATH + common install dirs; a missing Node produces an actionable error and the UI restart affordance). Verified: standalone handshake + resource present in a debug `.app`. All three PENDING open questions resolved (Node over Bun; whisper.cpp built-in Silero VAD; npx filesystem MCP per user decision) — see docs/context/decisions.md.
- **Files:** sidecar/package.json, src-tauri/{tauri.conf.json,src/sidecar.rs}, frontend/src/lib/agent-socket.ts, docs/SETUP.md, docs/context/decisions.md, PENDING.md.
- **Breaking:** packaged builds require Node ≥20 on the user machine (documented; same requirement npx connectors already impose).

## Landing page (GitHub Pages)
- **What:** static marketing site in `site/` — hero with an animated mock of the overlay pill/transcript/confirm card, problem framing, four-step how-it-works, six feature cards, privacy section, comparison table, open-source CTA. Built on the Halcyon Light tokens (Vox Blue, Hanken Grotesk + IBM Plex Mono via Google Fonts, borders-first elevation); single self-contained HTML file, responsive down to 375px, `prefers-reduced-motion` respected. Deployed by a new `deploy-pages` workflow (Actions → Pages) on pushes to main touching `site/`; user must set Pages source to "GitHub Actions" once.
- **Files:** site/{index.html,icon.png,og-logo.png}, .github/workflows/pages.yml.
- **Breaking:** none.

## Planner floor + ambient context v1
- **What:** `pickModel` no longer falls back to arbitrary non-tool-capable Ollama models (actionable error instead); system prompt gains three few-shot tool-call examples. New `context_snapshot` system action (NSWorkspace frontmost app name/bundle id) annotates each utterance with "(context: the app the user is in right now is X)" so "this app" references resolve — best-effort, never blocks a command.
- **Files:** sidecar/src/agent/{ollama.ts,graph.ts,runner.ts}, sidecar/src/core-bridge.ts, packages/protocol/src/index.ts, src-tauri/src/{commands/system.rs,core_client.rs}.
- **Breaking:** none.
